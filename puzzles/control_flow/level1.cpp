// What will this program debug?

int numPickups = 1;
int maxPickups = 3;
int numDrills = 5;
int maxDrills = 17;
bool justDrilled = false;

if(numPickups > maxPickups or justDrilled){
    DEBUG(("Back to base"));
    if(maxDrills > numDrills){
        DEBUG(("We gucci fam"))
    }
}

if(numPickups < maxDrills and justDrilled){
    DEBUG(("Geyser hit"))
}

